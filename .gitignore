# Build artifacts
dist/
build/
.build-cache/
*.dmg
*.app/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Signing secrets — never commit
*.p8
*.cer
*.p12

# macOS
.DS_Store

# Runtime data (these live in $HOME, but never commit them if they appear)
*.tmp
.photos_dedup_cache.json
.photos_dedup_decisions.json
.photo_saver_audit.jsonl
.photos_dedup.log
